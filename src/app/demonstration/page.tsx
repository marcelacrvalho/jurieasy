"use client";

import ContractWizard from '@/components/demonstration/ContractWizard';

export default function DemonstrationPage() {
    return (
        <div className="min-h-screen bg-gradient-to-br from-gray-50 to-blue-50">
            <ContractWizard />
        </div>
    );
}